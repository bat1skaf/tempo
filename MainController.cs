using System;
using Microsoft.EntityFrameworkCore;
// ...

namespace AppSocial {

		public class Login : HomeController {
    
    		static void Main(string[] args) {
        		public async AddCookieIntoDB();
        }
    		public async AddCookieIntoDB<CookieSettings> => {
        		if(context.Request.Cookies.ContainsKey("name")) {
            		string name = context.Request.Cookies.ContainsKey("name")
            }
            
            db.EnsureCreated();
            db.Add(Tokens, name)
        }
    }
}
